<Window x:Class="MandelbrotSet.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MandelbrotSet"
        mc:Ignorable="d"
        Title="MainWindow" Height="700" Width="940">
    <Window.DataContext>
        <local:MandelbrotViewModel />
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Button Click="LeftClick">Left</Button>
            <Button Click="RightClick" Grid.Column="1">Right</Button>
        </Grid>
        
        <local:MandelbrotSetView Grid.Row="1" x:Name="MandelbrotSetView" Map="{Binding Map}" MapWidth="{Binding MapWidth}" MapHeight="{Binding MapHeight}"/>
    </Grid>
</Window>
